This directory contains examples of what raw snippets look like:
- bandit0.txt
- bandit1.txt
- bandit2.txt

And what a variable yaml file looks like:
- bandit2.yaml

The metadata about the snippet and variables are saved in the ask database.

Notice the structure of variables within a raw snippet is `{{ VARNAME }}`.
